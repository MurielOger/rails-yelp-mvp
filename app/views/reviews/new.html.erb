<div class="container m-3">
  <div class="row">
    <div class="col-sm-6">
      <%= simple_form_for [@restaurant, @review] do |f| %>
        <%= f.input :content %>
        <%= f.submit "add a review" %>
    <% end %>
  </div>
  </div>
</div>
